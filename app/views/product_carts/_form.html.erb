<%= form_with(model: product_cart) do |form| %>
  <% if product_cart.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_cart.errors.count, "error") %> prohibited this product_cart from being saved:</h2>

      <ul>
        <% product_cart.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.label :cart_id, style: "display: block" %>
    <%= form.text_field :cart_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
